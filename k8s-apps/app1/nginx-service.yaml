apiVersion: v1
kind: Service
metadata:
  name: nginx-service
  namespace: app1
spec:
  selector:
    app: nginx
  type: NodePort
  ports:
    - protocol: TCP
      port: 80          # Service port (ClusterIP access)
      targetPort: 80     # Container port
      nodePort: 30001    # Fixed NodePort

